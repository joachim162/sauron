# PATokens.conf -- plugin definition file for plugin: PATokens
#
# $Id:$

# Name of the plugin.
$NAME = 'PATokens';

# Name of menu where to append commands.
$MENU = 'login';

# Commands to append to menu.
$MENUDATA = [
	    [],
	    ['Personal Access<br>Tokens', 'sub=Pl_PATokens_Manage',
	    'Pl_PATokens_Manage', 'Pl_PATokens_Create', 'Pl_PATokens_Revoke'],
	    ];

# Authorization level needed to use this plugin.
$ALEVEL = 0;

1;

# eof :-)
